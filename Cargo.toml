[package]
name = "olexsmir-xyz"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
mlua = { version = "0.11", features = ["lua51", "module"] }
pulldown-cmark = { version = "0.13.0", features = ["html"] }
